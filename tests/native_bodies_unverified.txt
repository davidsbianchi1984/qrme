# status: backlog — 0 rows
# ceiling: 0
#
# Native writes whose body the route cannot accept, which are real. There
# are none.
#
# 0.57.3 found seven distinct defects across the three shells, fixed one and
# recorded six at a ceiling of sixteen rows, each with the input it needed
# or the decision it was waiting on. 0.57.4 collected those inputs — a
# camera address, a department, a locality and an include-remote switch —
# and the file is empty.
#
# The ceiling is zero and should stay there. A shell that starts sending a
# field its route has no property for is sending it nowhere, and a required
# field it stops sending is a button that answers 422 on every press.
#
# Ratcheted: this list may shrink and must never grow.
