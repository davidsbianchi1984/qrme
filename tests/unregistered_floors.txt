# status: backlog — 39 rows
# ceiling: 39
#
# Every numeric floor in this suite still carrying its own literal: an
# `assert <something> > <int>` with the number written into the assertion.
#
# ## What this counts, and why counting it is the point
#
# A floor answers one question every run — is the number satisfied — and that
# is exactly the question that keeps passing after the number stops meaning
# anything. Asking the second question, *is it still near what it measures*,
# needs the measurement, and a literal inside an assertion has none attached.
#
#     asked     is the number satisfied
#     mattered  is the number still near what it measures
#
# 0.58.8 found the route reader's floor was written when the console was the
# only client. 0.58.9 found the localizer's was ten against nine hundred and
# forty-five. Applying the same standard to everything reachable found that
# every one of them had drifted below half:
#
#     l10n asked, per shell        10 against 945-961     ratio 0.01
#     l10n held, per shell         20 against 1087-1115   ratio 0.02
#     path literals, all surfaces  40 against 1407        ratio 0.03
#     console call sites          200 against 429         ratio 0.47
#
# Those four are now in `ratchets.py`, registered with the way to measure the
# same quantity, and audited every run. The rows below are what is left.
#
# ## How a row leaves this file
#
# Register it in `ratchets.py` with a `measure` callable and change the
# assertion to `ratchets.floor("name")`. The sweep finds bare literals only,
# so registering removes the row without anybody editing this list — and the
# registered floor is then checked against reality on every run.
#
# Not every row here is wrong. Some of these numbers are small fixed
# cardinalities that will never drift. Telling those apart from the decoration
# requires knowing what each one measures, which is the work of registering
# it, which is why they are counted rather than guessed at.
#
# The ceiling does not move up. A new bare floor is a new number nothing will
# ever audit.
#
#
# ## Not floors: a value that is not a size
#
# The sweep matches the *shape* `assert <something> >= <int>`. Some of what it
# caught is not a floor on anything that can grow — it is a value read out of
# a response or a fixture, compared against a number that means something else
# entirely.
#
#     asked     is this an `assert x >= <int>`
#     mattered  is the int a floor on something that can grow
#
# `status_code >= 400` is the clearest. 400 is not a size, there is no
# four-fifths of it to raise toward, and the comment beside it says what it is
# actually asking: an unconfigured door "refuses rather than pretending".
#
# `effective_age >= 41` is not even a minimum. The line above it fixes
# `base_age == 41` and aging on, so this asserts the effective age has moved
# *at or above* the base — a direction, written as a comparison.
# `remembered_turns >= 12` is about one simulation's basis, seeded by the test
# that reads it, and there is no estate-wide quantity for it to be four-fifths
# of.
#
# Each is exempted by `file.py: expr` rather than by line, because a line
# number is a fact about today's file — registering floors in this estate has
# already shifted unrelated rows by a line each, and an exemption keyed on a
# line would have silently started covering whatever slid into the slot.
#
#   test_steering_hub.py: hub['age']['effective_age'] >= 41
#   test_the_keys_the_till_and_the_lifeline.py: client.post('/auth/oauth/nobody/start', json={}).status_code >= 400
#   test_spec_mined.py: conditioned['basis']['remembered_turns'] >= 12
#
# One candidate was deliberately *not* exempted. `test_seed_backfill.py`'s
# `out['repaired'] >= 34` counts profiles a repair pass fixed, and that
# number tracks the seed catalogue, which grows. It reads like the others and
# is not one of them, which is the reason this list is prose a person edits
# rather than a rule a sweep applies.
#
# `test_every_exempted_comparison_is_still_there` checks each is really
# written somewhere, and `test_a_count_can_never_be_exempted` checks the
# direction that matters: a comparison against `len(...)` or `sum(...)` is a
# count of a swept surface, is always a floor, and may not be excused here
# however convincing the prose above it. An exemption nothing verifies is a
# place to hide the numbers this file is meant to find.
test_a_memorial_does_not_keep_posting.py:160: len(found) >= 8
test_a_refusal_whose_english_is_not_a_constant.py:248: len(calls) >= 15
test_a_screen_nothing_opens.py:170: len(found) >= 5
test_it_observes_and_talks.py:93: len(never[thing]) > 60
test_seed_backfill.py:86: out['repaired'] >= 34
test_starter_profiles.py:15: len(industries) >= 30
test_termination_revokes_more_than_the_owners_token.py:154: len(found) >= 7
test_the_body_market.py:65: len(c['robots']) >= 30
test_the_body_market.py:67: len(c['by_maker']) >= 15
test_the_body_the_native_clients_send.py:358: matched >= 155
test_the_body_the_route_requires.py:246: len(writes) >= 185
test_the_body_the_route_requires.py:247: len(readable) >= 155
test_the_body_the_route_requires.py:254: len(models) >= 160
test_the_body_the_route_requires.py:264: matched >= 150
test_the_desktop_and_the_phone_say_different_things.py:302: len(shared) > 120
test_the_files_the_release_never_touched.py:272: len(sorted(REPO.glob('native/ios/Sources/**/*.swift'))) >= 40
test_the_files_the_release_never_touched.py:273: len(sorted(REPO.glob('native/android/**/*.kt'))) >= 10
test_the_keys_the_android_client_reads.py:359: len(reads) >= 165
test_the_keys_the_android_client_reads.py:360: sum((len(k) for _, k in reads)) >= 370
test_the_keys_the_android_client_reads.py:383: len(driven) >= 80
test_the_member_that_isnt_there.py:343: len(keys) >= 12
test_the_member_that_isnt_there.py:344: len(used) >= 10
test_the_nav_is_translated_and_nothing_behind_it_is.py:229: len(nav) >= 40
test_the_nav_is_translated_and_nothing_behind_it_is.py:255: len(nav_ids) >= 40
test_the_objector_cannot_read_their_own_case.py:151: len(handlers) >= 8
test_the_refusal_names_the_field_on_the_form.py:104: len(found) >= 200
test_the_refusal_that_handed_the_body_back.py:157: len(found) > 150
test_the_refusal_that_handed_the_body_back.py:161: reached > 100
test_the_shape_the_client_expects.py:309: len(driven) >= 30
test_the_shape_the_client_expects.py:322: len(names) >= 490
test_the_shape_the_console_expects.py:457: len(driven) >= 60
test_the_shape_the_swift_client_expects.py:228: len(driven) >= 25
test_the_shells_table_answers_every_reader.py:107: len(_rows(shell)) >= 200
test_the_starters_know_their_trade.py:69: len(d['expertise']) >= 350
test_the_starters_know_their_trade.py:72: len(d['services']) >= 250
test_the_starters_know_their_trade.py:140: len(tags) >= 8
test_the_tabs_are_translated_and_the_screens_are_not.py:554: english + calls >= 50
test_the_tabs_are_translated_and_the_screens_are_not.py:775: len(rows) >= 10
test_two_questions_a_mark_answers.py:196: len(why) > 40
