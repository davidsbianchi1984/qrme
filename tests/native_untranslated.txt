# status: floor — 0 rows
#
# The accountless screen, on the three shells that are not a browser.
# Checked by test_the_strangers_language_on_a_phone.py, and ratcheted.
#
# **Empty, as of 0.40.6.** All three screens read their strings from their
# shell's L10n table in the language the device is carrying, and the wording is
# the console's `pub.*` rows ported rather than translated a second time.
#
# The file stays because the ratchet stays: a shell that grows a new English
# sentence on this screen has to write it down here, and the ceiling below does
# not move up.
#
# What holds the floor is not this file. Three lines could always have been
# deleted from it with all three screens still in English —
#
#     asked     is the backlog written down and shrinking
#     mattered  did anything get translated
#
# — so `test_no_accountless_screen_has_english_of_its_own` now reads the
# screens themselves, using the sibling guard's own extraction patterns so the
# two cannot disagree about what an English string is.
#
# ceiling: 3
