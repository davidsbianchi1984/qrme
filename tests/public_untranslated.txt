# English left on the public screen — the surface for people with no
# profile. Checked by test_the_stranger_has_a_language_too.py, both
# directions, and ratcheted.
#
# This list used to hold five sentence fragments and describe them as the
# hard remainder. They were not the remainder. They were what a regex over
# the source happened to be able to see.
#
# `scripts/jsx-text.mjs` asks TypeScript's own parser instead, and the real
# figure was twenty-five: an entire explanatory paragraph in the mark pane,
# a whole sentence about signing in, the signature line, "Also present on:",
# "Forms:", the window-match line, and the produced-by line — none of which
# the old pattern could reach, because every one of them wraps an
# interpolated value and the pattern excluded braces outright.
#
# All twenty-four of those are now translated into all ten languages, as
# whole sentences with their values named (`{now}`, `{stored}`) and put back
# by `fill` — so each language decides where its own grammar wants them.
#
# What is left is the brand.
#
# ceiling: 1
QRME
