# status: floor — 4 rows
#
# English left on the surface a person meets before any account exists.
# Checked by test_the_stranger_has_a_language_too.py, both directions, and
# ratcheted.
#
# **Finished.** Both screens `App.tsx` can render before a profile exists —
# `Public.tsx` and `Onboarding.tsx` — read their text from `l10n.ts` in the
# language the visitor's browser asks for. What is left below is not prose: a
# product name, a punctuation mark, an example address and an example code,
# strings that are the same in every language. This is the floor, not a
# backlog.
#
# ## How this file read until 0.40.7
#
# It opened with a paragraph explaining that `Onboarding.tsx` — the screen
# every person in the world meets first — carried forty-odd English strings,
# that translating them was "its own round", and that a half-translated sign-up
# form would be worse than an English one. All of that was true when it was
# written.
#
# `The screen everybody meets first` translated them. `The pre-session backlog
# reaches its floor` took the count to four and appended the floor note —
# *below* the stale paragraph, which nobody struck. So the file held two
# statements about itself with the false one first, and a reader taking the
# first claim a file makes was told a cleared backlog was still owed.
#
#     asked     is the record complete
#     mattered  does the record still describe the code
#
# The numbers were right the whole time. The prose around them had outlived the
# thing it described, which is why every ratchet now leads with a `# status:`
# line whose count is checked against the rows beneath it.
#
# ceiling: 4
Onboarding: .
Onboarding: 123456
Onboarding: you@example.com
Public: QRME
