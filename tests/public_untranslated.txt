# English left on the public screen — the surface for people with no
# profile. Checked by test_the_stranger_has_a_language_too.py, both
# directions, and ratcheted.
#
# Every line below is a **sentence fragment**, and that is the point. The
# public screen interpolates values mid-sentence — the profile's status, the
# window counts, the profile id — so what a naive extractor pulls out is
# "from this moment. It was" and ", and if the objection is dismissed it goes
# back to exactly that."
#
# Those are not translatable units. Handing them to a translation table one
# at a time produces word-order nonsense in every language that does not
# happen to share English's, because the interpolated value belongs somewhere
# else in the sentence. Translating them *as listed* would be worse than
# leaving them in English: it would look done and read broken.
#
# Fixing them properly means restructuring the JSX so each sentence is one
# template with its values named — which is a change to the screen, not to a
# string table, and is its own round.
#
# The five whole sentences that were here have been translated into all ten
# languages and are gone from this list.
#
# ceiling: 5
, and if the objection is dismissed it goes back to exactly that.
Produced by a QRME synthetic profile —
This says nothing about whether a person wrote it. It says no profile
from this moment. It was
has stamped work that shares enough wording with it.
