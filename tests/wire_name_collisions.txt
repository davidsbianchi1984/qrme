# status: backlog — 0 rows
# ceiling: 0
#
# One wire field name carrying more than one type across this product's API.
#
# `spoken` was the row that made this file. It meant three things at once:
# the CPR playbook steps read aloud (a string list), the model's rewritten
# presence line (a string), and whether a beat was said out loud (a boolean).
# The Windows client had already forked it into three records without anybody
# noticing what the forking meant, and TypeScript could not fork it — one
# interface `extends` another there — so `tsc` had been failing on `main` and
# nothing in any suite ran `tsc`.
#
# ## after 1.8.9 — the twenty-one close
#
# Every backlog row resolved in one round, the same way JIM closed its
# twenty and PDI its two: the count stops wearing its collection's name
# (comments_count, subscribers_count, entries_count, items_count,
# turns_count, messages_count, offerings_count, sources_count), and a
# record that shared a name with a different shape gets its own
# (deal_items, cards, seats, thread_turns, summoned, found,
# app_providers, signin_providers, rating_summary, scopes, ground_rules,
# page_theme, composition_sources, timeline_events, audit_events,
# goal_amount, watermark_line, message_rows / post_rows / source_rows).
#
# Two rows closed inside the Windows client without touching the wire,
# because they were never two wire types at all: `display` was one shape
# (watermark.design()) declared twice as WatermarkDisplay and
# WatermarkDesign, and `watermark` on /perceive was the same stamp block
# the posts carry, declared again as PerceiveWatermark with a `line` field
# the route never sends. Unifying the records fixed the count and deleted
# a fiction in the same edit. FeedbackOut.rating was the other fiction:
# the feedback route echoes the engagement record, never the rating.
# `profiles` closed both ways at once: browse and summon got their own
# names (found, summoned), and the roster and the account's held list
# turned out to be one function serving one shape through two doors —
# one record now decodes both.
#
# One name arrived while closing: renaming FeedbackOut's field exposed
# `contributed` carrying bool beside ContributionRow[] — the feedback
# route's bool is now `cloud_contributed`, so the close did not mint the
# next collision on its way out.
#
# Ratcheted at zero: `test_no_wire_name_carries_two_types` fails on the
# first name that ever carries two types again, and this file may not grow.
