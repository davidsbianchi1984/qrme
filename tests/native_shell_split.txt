# status: floor — 0 rows
#
# Rows on which this product's three shells say different things to each other.
# Checked by test_the_three_shells_say_the_same_thing.py, matched exactly in
# both directions, in two forms: `key:` where two shells hold one key and
# disagree, and `english:` where all three hold one English string under
# whatever keys and share no wording.
#
# ## The third axis, and how large it turned out to be
#
# 0.48.0 compared keys inside one table. 0.48.1 compared the console's table
# with each shell's. Both ended by naming this axis as the next thing, because
# each had rows it could not reconcile: the shells disagreed with each other,
# so there was no single native wording to adopt.
#
#     asked     does each table agree with itself, and with the console
#     mattered  do the three shells agree with each other
#
# Measured for the first time in 0.48.2, it held **one row** in this product:
#
#     action.sign_out, Portuguese — the phones said *Sair* and the Windows
#     shell said *Terminar sessão*. *Sair* is leave or exit; ending a session
#     in pt-PT is *terminar sessão*, so the odd shell out was the right one and
#     the other two moved to it.
#
# 1056 keys are held by two or more shells here and 972 English strings by all
# three. One disagreement in either set. The sibling product held three, and
# the vault none.
#
# Recording that plainly is the point. Two rounds pointed at this axis as the
# next large thing and it was not large, and a guard built on the assumption
# that it would be would have been built to find much more than exists.
#
# ## Majority, not a favourite
#
# Where two shells agree and one does not, the odd one out is the drift and the
# fix follows the two. Where all three differ there is no majority to follow,
# the row is a judgement rather than a defect, and it belongs here instead —
# the rule `native_split_wordings.txt` leads with. No row has needed that yet.
# The check exists so the first one is recorded rather than guessed at.
#
# ceiling: 0
