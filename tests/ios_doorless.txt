# Deferred, not undecided.
#
# GET /profiles/{profile_id}/media lists what a profile has uploaded. The
# console got it in the same round it got the profile homepage screen, which
# is the only place it belongs: on a phone it would be a call with nothing
# behind it, because the phones have no screen for somebody else's page yet.
# A binding is not a door, and adding one here to empty this file would be
# the exact move this file exists to prevent.
#
#     asked     can the phone reach the route
#     mattered  is there anywhere on the phone for it to land
#
# It goes when the homepage screen reaches the shells — one native round,
# tracked, and this line comes out with it.
GET /profiles/{profile_id}/media
