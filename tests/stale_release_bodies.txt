# status: backlog — 1 row
#
# frozen-opens: Nine rounds, one question
#
# Published releases whose body still opens with the frozen v0.24.0 prose.
# Checked by `test_the_release_bodies_that_say_nothing.py` here, and against
# GitHub by `.github/workflows/release-bodies-sweep.yml`.
#
# ## What these rows are
#
# `RELEASE_NOTES.md` was last written for v0.24.0, and `sync-release-notes.yml`
# published it verbatim over every release body after it. 412 of 530 releases
# across the three products, one identical body spanning 134 tags. The file and
# the workflow are deleted as of 0.60.8.
#
#     asked     which workflow owns the release body
#     mattered  does the body say what shipped
#
# ## How staleness is decided, and the two ways it was decided wrongly
#
# A body is stale when its prose **opens with** the line in `# frozen-opens:`
# above. That line is per product and lives here, so the workflows read it
# rather than carrying a copy.
#
# The first guards written for this asked whether the body contained the string
# `414 tests passing` anywhere. That was wrong twice over, and both showed up on
# the first real backfill:
#
#   * It is **PDI's** number. QRME's and JIM-mini's frozen bodies carry their
#     own counts, so the sweep reported zero stale in two products while 112
#     and 113 remained — a check that passed by being blind.
#   * It matched any body that merely **quoted** the phrase. PDI's own
#     `app-v0.60.8` notes describe this finding, so a correct release failed
#     the check for saying what it fixed.
#
#     asked     does this body contain the sentinel
#     mattered  does this body open as the frozen one does
#
# Opening-line matching answers the second question. A release note is free to
# discuss the frozen body; it may not *be* it.
#
# ## What a row means
#
# One row per stale release. A row leaves when that release's body is rebuilt
# from its own CHANGELOG entry by `backfill-release-bodies.yml`. The ceiling may
# only fall.
#
# A release deliberately left alone **keeps its row with the reason on it**,
# rather than becoming an exemption in code where nobody reads it again.
# `app-v0.24.0` is that row and always will be: its body *is* the v0.24.0
# notes, right for it and wrong for the 411 that inherited it.
#
#
# ## 412 → 1
#
# Every release that inherited the frozen body has been rebuilt from its own
# CHANGELOG entry. What is left is the one row that was never a defect.
#
# Two things went wrong on the way and both are worth keeping, because both
# were checks that reported success while doing nothing:
#
#   * the first staleness test looked for `414 tests passing` anywhere in a
#     body. That count is PDI's, so the sweep read zero stale in the two
#     products where a hundred and twelve remained — and it also matched any
#     release note that *quoted* the phrase, failing a correct release for
#     describing the defect it fixed. Staleness is decided by how the prose
#     opens now, from the `# frozen-opens:` header above.
#   * the backfill then walked this record rather than the releases. The record
#     lives on the default branch and the releases live on GitHub, so the two
#     drift the moment a batch lands. Three runs spent their whole limit
#     rewriting releases repaired two batches earlier, reported success, and
#     never reached the last eleven. It reads each release before writing now.
#
#     asked     did the run succeed
#     mattered  did the run change anything
#
# This file stays at one row. It is not empty and will not become empty: an
# empty record would say *no release was ever stale*, which is the opposite of
# what happened here.
#
# ceiling: 1
app-v0.24.0  keep — this body IS the v0.24.0 notes, and is correct for it
