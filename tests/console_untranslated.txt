# status: backlog — 58 rows
#
# ## 0.69.1: the reader learned to see a sentence chosen at render time
#
# A field report on PDI — "It seems to be blocking the PDI menus" — began
# with a pill sitting on a tab bar and ended here. The pill's own words,
# *vault answering*, were English on a console that translates everything
# else, and the extractor behind every count in this estate reported the
# file clean: `{ok ? "vault answering" : "vault degraded"}` is a
# `ConditionalExpression`, not a `JsxText` node, so a sentence chosen at
# render time was text to the reader and nothing at all to the reader of
# the reader.
#
#     asked     what text does this file place between its tags
#     mattered  what words does this screen put on the glass
#
# The extractor now reads string literals in child position — both branches
# of a ternary, either side of `&&`, the pieces of a concatenation — and
# skips anything with no letter in it, because a spacer is drawn but is not
# a word. It still refuses call arguments, so `t("key", lang)` stays a key.
#
# 57 rows below are what that widening found. They are not new English;
# they are English this file has been unable to see for as long as it has
# existed, and the honest thing is the number rather than the floor it was
# resting on. The direction of travel is unchanged — the rows come off one
# screen at a time — but the count they come off is the true one.
## English on QRME's gated console. Checked by
# test_the_nav_is_translated_and_nothing_behind_it_is.py, both directions,
# and ratcheted.
#
# ## What this file was for
#
# The sidebar had forty-six labels, every one translated into ten languages,
# and forty-six screens behind those labels on which every string was
# English. `l10n.ts` said so in its own first line — "chrome localization for
# the desktop console" — and three rounds of language audit read that
# sentence as a scope rather than as a finding. Each widened correctly inside
# it: Public.tsx, then Onboarding.tsx, then the native shells. None asked
# what was on the other side of the forty-six translated words.
#
#     asked     is the chrome localized
#     mattered  is anything behind the chrome localized
#
# That was worse than a console uniformly in English, which is why it was
# recorded here rather than folded into public_untranslated.txt. A console
# with no translations tells a Spanish reader the truth on the first screen.
# That one put "Mercado", "Amigos" and "Ajustes" in the sidebar — the app
# answering in their language — and then handed them 1576 English strings the
# moment they clicked. The promise was made by the only part that was kept.
#
# ## What it is for now
#
# The count reached its floor in 0.46.3, over twenty-one releases and about
# 1500 keys. It is no longer a backlog; it is a floor with one row on it, and
# the ratchet holds it there in both directions — a new English string on a
# gated screen fails the suite, and so does a row here that has been fixed
# and not struck.
#
# Two corrections are worth keeping, because both were the count lying in a
# direction that flattered nobody.
#
# **0.30.10.** 117 rows here were punctuation, not English: a bare `:`, a
# `·`, a `%`, a `⚠`. The extractor kept them under a stated rule — "a
# separator somebody reads" — which conflated *rendered* with *unreadable to
# a non-English speaker*. There is no Portuguese for `·`.
#
#     asked     is this string rendered to somebody
#     mattered  is this string one a non-English reader cannot read
#
# The count this file exists to state honestly was overstated by that much.
#
# **0.45.9.** The one row below is deliberate and stays. `AI ·` is quoted,
# not written: the sentence beside it on TheMark says the line comes back
# with that in front of whatever you type, and the server hardcodes those two
# characters into `design.line`. Translating the quotation to `IA ·` would
# put a word on the screen that the product never produces — the paragraph
# would be describing a designation nobody is shown. It is quoted the way
# `409` and `#tag` are quoted, so it is English on purpose and recorded as
# such. A floor of zero would have been a nicer number and a less honest one.
#
#
# ceiling: 58
#
# The ceiling was 1 and is 58: the reader widened, not the console.
# Every row it gained was already on the glass. Raising a ceiling is the
# one move this estate treats as a claim rather than a chore, so it is
# written here beside the reason, and the next rounds take it back down.
Assist: it
Assist: this content does not match the credential
Assist: you
Assist: · marked as AI-generated
Assist: · not AI-marked, because you made it
Desk: Live.
Desk: No person attested.
Desk: Not live.
Desk: · CLAIM BURNED
Identity: Nothing in it yet.
Identity: Publish my name again
Identity: Withhold my name
Identity: — who checked is part of the record, not a footnote.
Lobby: s
Referrals: not opened yet
Referrals: nothing
Referrals: the patient
Referrals: the profile
Referrals: you
Referrals: · near you
Referrals: — not enough for a referral yet
Remainder: , including the ones only a rated profile has.
Remainder: . The rated-only ones are listed and refused here, rather
Remainder: left this machine
Remainder: never left this machine
Remainder: nothing needed stripping
Remainder: than hidden, so the refusal has something to point at.
Remainder: · 18+ only
Remainder: · folded in
Remainder: · free
Robots: Hide the full list
Robots: Nothing bound yet.
Robots: Show the full list
Robots: Sign in as an owner.
Robots: on the profile itself
Robots: · cannot speak in character
Robots: · free
Robots: — cannot be bound yet
Selling: · a buyer may derive an agent
Selling: · an agent has been derived from it
Selling: · consult only
Selling: · nothing derived yet
Selling: · revoked
Settings: Clear key
Settings: Save
Settings: Save key
Settings: Save mail settings
Settings: Saved ✓
Settings: Saving…
Settings: Send test email
Settings: Sending…
TheMark: AI ·
Workshop: No vault here, so the result stays in the clear.
Workshop: Nothing was transmitted anywhere.
Workshop: Something was transmitted externally.
Workshop: The result is sealed in the vault.
Workshop: · answers for itself
Workshop: · relays only
