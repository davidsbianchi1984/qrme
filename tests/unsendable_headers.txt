# status: backlog — 1 row
# ceiling: 6
#
# Client/header pairs that are deliberate: a route a client has a door to
# whose header that client is not meant to present. A `*` client means no
# client at all should send it.
#
# A row here needs a reason. "The phone does not do that" is usually the
# defect rather than the reason, which is how 0.59.6 started next door: a
# vault under customer custody required a header no client sent, and the
# comparison between clients passed because they agreed.
#
# x-signup-key is the genuine article. An operator who sets QRME_SIGNUP_KEY
# on a published instance is closing registration to everybody, which is
# what the switch is for — "unset = open, the right default on a LAN". A
# client that could present it would be a client that reopens the door the
# operator shut.
*: x-signup-key
