<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net8.0-windows10.0.19041.0</TargetFramework>
    <TargetPlatformMinVersion>10.0.17763.0</TargetPlatformMinVersion>
    <!-- What Windows shows in the file's Properties, and the only
         thing a problem report from an install can be pinned to.
         Absent until 0.57.7, which meant every build since the
         shell was written reported the 1.0.0 default. -->
    <Version>0.95.0</Version>
    <AssemblyVersion>0.95.0.0</AssemblyVersion>
    <FileVersion>0.95.0.0</FileVersion>
    <RootNamespace>QrmeStudio</RootNamespace>
    <ApplicationManifest>app.manifest</ApplicationManifest>
    <Platforms>x64;ARM64</Platforms>
    <RuntimeIdentifiers>win-x64;win-arm64</RuntimeIdentifiers>
    <UseWinUI>true</UseWinUI>
    <Nullable>enable</Nullable>
    <LangVersion>latest</LangVersion>
    <!-- Unpackaged: runs as a plain .exe, and (unlike a packaged MSIX app) is
         not subject to loopback restrictions, so http://127.0.0.1 works. -->
    <WindowsPackageType>None</WindowsPackageType>
    <WindowsAppSDKSelfContained>true</WindowsAppSDKSelfContained>
    <EnableMsixTooling>false</EnableMsixTooling>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.WindowsAppSDK" Version="1.6.240923002" />
    <PackageReference Include="Microsoft.Windows.SDK.BuildTools" Version="10.0.26100.1742" />
  </ItemGroup>

</Project>
